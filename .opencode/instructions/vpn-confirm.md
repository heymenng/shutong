# 翻墙/VPN 使用铁律

1. **默认关闭**：本机翻墙软件（如 Clash Verge）不得开机自启、不得在后台常驻。
2. **开启前必须确认**：在启动、打开、重新加载任何 VPN / 代理 / 翻墙工具之前，必须先向师父请示并获得明确同意。
3. **上云同步前检查**：凡是需要 SSH / SCP / rsync 到大陆阿里云服务器（114.55.9.27）的操作，必须先确认翻墙软件已关闭、流量走本地直连；若未关闭，先请示师父再操作。
4. **禁用后不复原**：除非师父明确指令，否则不得把已禁用的翻墙自启重新打开。
